# Fonts

Fontes self-hosted do projeto.

A fonte será escolhida e configurada pelo agente durante a criação do layout.

**Formato:** Apenas `.woff2` (melhor compressão)

**Configuração:** `font-display: optional` (nunca `swap`)

