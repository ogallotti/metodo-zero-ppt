[build]
  command = "npm run build"
  publish = "dist"

[dev]
  publish = "dist"
  port = 8888
  autoLaunch = false

# Otimização de imagens
[images]
  remote_images = ["https://.*"]

# Formulários
[functions]
  directory = "netlify/functions"
